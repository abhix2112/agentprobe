[pytest]
# Put the engine package root (this dir) on sys.path so `import app` works under
# the bare `pytest` console script, not just `python -m pytest`. Without this CI
# fails to collect tests with "ModuleNotFoundError: No module named 'app'".
pythonpath = .
testpaths = tests
